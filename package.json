{
  "name": "react",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://vitalik7.github.io/table-data-editor/",
  "dependencies": {
    "@material-ui/core": "^1.0.0",
    "material-ui": "^0.20.0",
    "muicss": "^0.9.39",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.0.17"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "npm-run-all": "^4.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "rm -rf ./dist/ && mkdir dist && cp -r ./build/ ./dist",
    "deploy": "gh-pages -d ./dist",
    "publish": "run-s build deploy"
  }
}
